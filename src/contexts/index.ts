/**
 * Contexts导出文件
 */

export { 
  GlobalSettingsProvider, 
  useGlobalSettings,
  useChatViewMode,
  usePhoneSilenceDuration,
  useInputMode,
  useSelectedTeacher,
  useVoiceSpeed,
  useTTSConfig,
  type GlobalSettings
} from './GlobalSettingsContext';
